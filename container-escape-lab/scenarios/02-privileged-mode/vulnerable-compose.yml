services:
  lab:
    image: alpine:3.20
    container_name: lab-privileged
    privileged: true
    command: ["sh", "-c", "apk add --no-cache bash findutils && tail -f /dev/null"]
    volumes:
      - ./exploit.sh:/lab/exploit.sh:ro
