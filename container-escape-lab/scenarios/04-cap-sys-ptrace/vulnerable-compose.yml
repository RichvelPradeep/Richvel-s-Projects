services:
  lab:
    image: alpine:3.20
    container_name: lab-sys-ptrace
    cap_add: [SYS_PTRACE]
    pid: host
    command: ["sh", "-c", "apk add --no-cache bash procps && tail -f /dev/null"]
    volumes:
      - ./exploit.sh:/lab/exploit.sh:ro
