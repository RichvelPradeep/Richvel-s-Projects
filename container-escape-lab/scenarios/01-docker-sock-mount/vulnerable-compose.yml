services:
  lab:
    image: alpine:3.20
    container_name: lab-docker-sock
    command: ["sh", "-c", "apk add --no-cache bash curl docker-cli && tail -f /dev/null"]
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./exploit.sh:/lab/exploit.sh:ro
