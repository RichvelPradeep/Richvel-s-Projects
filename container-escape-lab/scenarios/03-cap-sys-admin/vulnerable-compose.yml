services:
  lab:
    image: alpine:3.20
    container_name: lab-sys-admin
    cap_add: [SYS_ADMIN]
    command: ["sh", "-c", "apk add --no-cache bash util-linux && tail -f /dev/null"]
    volumes:
      - ./exploit.sh:/lab/exploit.sh:ro
